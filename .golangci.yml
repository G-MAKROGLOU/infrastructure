linters:
  enable:
    - misspell
    - unconvert
    - whitespace
    - nilerr
    - bodyclose

linters-settings:
  misspell:
    locale: US

issues:
  exclude-rules:
    - path: _test\.go
      linters:
        - errcheck

run:
  go: "1.25"
  timeout: 5m
